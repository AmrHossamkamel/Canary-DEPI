[defaults]
inventory = inventory/hosts.ini
host_key_checking = False
retry_files_enabled = False
roles_path = roles
private_key_file = ~/.ssh/depi.pem
remote_user = ec2-user
interpreter_python = auto
